# Now that the module is saved in the correct location,
# we can run the command as if it were a native shell command.


# This should fail with an error
# Highlight and run only this:
Get-CorpCompSysInfo -hostname localhost

# This should run
# Highlight and run only this:
Get-CorpCompSysInfo -hostname LON-DC1


